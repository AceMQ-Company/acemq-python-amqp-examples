# The examples that need the library's main branch rather than 0.3.0.
#
# One directory per line. Everything not listed here resolves the released
# package from https://acemq.org/pypi/, which is what a reader will be told to
# install and therefore what most of these examples should be proving works.
#
# CI checks that every line names a directory that exists, so this cannot rot
# into a list of examples nobody has any more. Deleting a line is what promotes
# an example to the release; when 0.4.0 is out, every line here goes.
basic/05-serialization
intermediate/05-scheduler
intermediate/06-saga
advanced/01-encrypting-payloads
advanced/02-development-certificates
advanced/04-tracing
