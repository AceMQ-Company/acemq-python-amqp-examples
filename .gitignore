.venv/
.venv-*/
__pycache__/
*.pyc
.ruff_cache/
# Written by advanced/02-development-certificates, and by nothing that should
# be committed: a generated authority's private key sits next to its
# certificate.
/certs/
*.crt
*.key
*.pem
# The examples that keep state keep it in a file beside them.
*.db
# The claim-check store, written and removed by intermediate/07-claim-check. It
# is here for the run that fails before it gets to the removing.
/intermediate/07-claim-check/payloads/
.DS_Store
