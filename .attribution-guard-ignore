# One grep -E pattern per line; # for comments.
#
# A virtualenv holds other people's packages, and their documentation is theirs
# to word. Scanning it says nothing about what this repository wrote. There are
# two here — the released library and the library's main branch — so the
# pattern covers both.
^\./\.venv
